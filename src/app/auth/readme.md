# Auth

Handles login, registration, password reset, auth guards, and user profile UI.

Main areas:
- `pages/` contains routed auth screens.
- `components/` contains reusable form and user components.
- `services/` contains auth state and API session handling.
- `middleware/` contains route guards.
