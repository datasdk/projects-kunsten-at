# Media Audio

Contains the audiobook feature.

Main areas:
- `pages/` contains routed audiobook list and player pages.
- `components/` contains the audio list and player UI.
- `services/` contains audiobook playlist and sound API access.
- `models/` contains audio-related data models.
